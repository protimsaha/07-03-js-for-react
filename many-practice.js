const gadgets = [
    { name: 'phone', price: 12000 },
    { name: 'headphone', price: 120 },
    { name: 'usb', price: 20 }
]

if (gadgets.name !== 'phone') {
    // console.log(gadgets)
}

const newGadget = { name: 'radio', price: '400' }
let market = `Hi, today I bought a ${gadgets.filter(gadget => gadget.name.includes(p))}`
// console.log(market)

const latest = [...gadgets, newGadget]
// console.log(latest)

const person = { name: 'Babe' }
console.log(person.name)
